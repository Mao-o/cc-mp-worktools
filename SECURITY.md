# Security Policy

## 対象バージョン

サポート対象は **`main` の最新 commit** だけです。plugin は `plugin.json` の
`version` を bump した時点で配布されるため、修正は常に「最新版へ更新」で受け取ります。
過去バージョンへのバックポートはしません。

| 対象 | サポート |
|---|---|
| `main` の最新 | あり |
| 過去の tag / 古い installed version | なし (更新してください) |

## 報告方法

**GitHub の private vulnerability reporting を使ってください。** リポジトリの
**Security → Report a vulnerability** から非公開で報告できます。

これが使えない場合は public issue でも受け付けますが、次を守ってください:

- **実際の機密値を貼らない** (鍵・トークン・実在するアカウント名・社内パス)。
  再現に必要ならダミー値に置き換えるか、「`*.pem` を Read した」のように形だけ書く
- 影響を受ける plugin 名と version (`plugin.json` の `version`) を書く
- 再現手順は最小限で十分です。exploit の完成形は不要

## 対応方針

個人メンテナンスのプロジェクトなので SLA はありません。実際の運用は次のとおりです:

1. 受領したら内容を確認し、再現できるかを返信します
2. 再現した場合は、まず**回帰テストを追加**してから修正します (同じ穴を塞いだことを
   機械的に検証できる状態にするため)
3. 修正は該当 plugin の patch / minor リリースとして出し、その plugin の
   `CHANGELOG.md` に記載します。**どのような値が漏れ得たか**は書きますが、
   報告者の環境に由来する具体値は書きません
4. 報告者の氏名・ハンドルのクレジットは、希望を確認してから記載します

## 脅威モデルの前提 (報告前に読んでください)

この marketplace の hook は **「うっかり」を減らす予防線**であって、
**敵対的な攻撃者を止めるサンドボックスではありません**。以下は設計上の選択であり、
脆弱性としては扱いません。

### `sensitive-files-guardrail`

- 目的は **Vibe Coding 中のうっかり露出の予防**で、**敵対的バイパス対策ではありません**。
  hook を通らない経路 (利用者自身が書いたスクリプト、エンコードした間接参照、
  Claude Code 以外のツール) で機密ファイルを読めることは既知かつ想定内です
- PreToolUse hook は**コマンド文字列の静的解析**です。実行時に組み立てられる
  コマンドや解釈系を経由した参照は原理的に判定できません
- repo 同梱のパターン tier (`.claude/sensitive-files-guardrail/patterns.txt`) は
  commit された内容がそのまま効くため、**clone した repo の `!` 行が保護を弱め得ます**。
  これは「貢献者と CI で除外を共有できる」ことの裏側で、意図した trade-off です
  (利用者個人のファイルが常に上位なので個別に打ち消せます)。第三者のリポジトリを
  開くときは、このファイルの中身も差分レビューの対象にしてください

一方で、次は**脆弱性として報告してください**:

- 機密値が hook の出力 (deny reason / redaction 済みのはずの本文) に残る
- ログファイルに鍵名・パス・値が書かれる (書かない設計です)
- 通常の Read / Bash / Edit / Write 経路で、既定パターンに一致するファイルが
  検出されずに素通りする

### `verify-cloud-account`

- 目的は**アカウント誤爆の予防**で、認証情報そのものの保護ではありません。
  利用者が hook を無効化すれば検証は走りません (`VERIFY_CLOUD_ACCOUNT_MODE=off`)
- Firebase の configstore などトークンを含むファイルを読みますが、使うのは
  プロジェクト識別子だけで、内容をメッセージに出しません。**ここに値が漏れる経路が
  あれば報告対象です**

### `external-ai-assist`

- この plugin は **plan 本文や `git diff` を第三者の AI CLI に渡します** (詳細は
  [README.md](README.md) の「Privacy & data flow」)。送信そのものは仕様です
- 送信内容の除外規則は「plugin が渡す差分」の範囲を絞るだけで、起動された外部 CLI が
  作業ツリー内の他のファイルを自ら読むことは防げません
- 第三者 CLI 側のデータ保持・学習利用の方針は各サービスの規約に従います。ここで
  管理できる範囲外です
- **既定で無効化されていない除外パターンの穴** (例: `.env` が除外されずに送信される)
  は報告対象です

### `verify-plugin-release`

- この hook は `gh pr create` / `gh pr ready` の前に、**repo 内のテストコードと、repo の
  設定ファイル (`.claude/verify-plugin-release.json`) の `test_command` を実行します**。
  実行に permission prompt は挟まりません。`gh pr create` を permission rule で許可して
  いる場合、それらも暗黙に実行されることになります。信頼できない repo では
  `VERIFY_PLUGIN_RELEASE_MODE=off` で無効化してください
- 目的は**リリース漏れの予防**で、PR の作成を強制的に禁止する仕組みではありません
  (環境変数で止めずに通すこともできます)
- 検査が完了できないのに PR 作成が素通りする経路 (fail-open) は報告対象です

### 共通

- hook の出力は Claude Code の会話に入ります。**Claude Code 本体が Anthropic の API へ
  送ること**は、どの plugin の設定でも変えられません
- テスト fixture に含まれるダミー鍵・サンプル `.env` は**合成データ**です。実鍵では
  ないため報告不要です

## 依存関係

hook は Python 3.11+ の標準ライブラリと `git` だけで動きます (サードパーティの
Python パッケージを追加しません)。したがって依存パッケージ由来の脆弱性通知は
対象外です。
